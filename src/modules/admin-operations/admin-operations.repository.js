import { supabaseServerRequest } from "@/src/config/supabase/server";
export async function listAdminOrders(channel){const p=new URLSearchParams({select:"id,order_number,channel,status,payment_status,payment_method,fulfillment_type,subtotal,delivery_fee,total,command_label,created_at,closed_at,customer:customers(name,phone),table:dining_tables(table_number),items:order_items(id,product_name,quantity,subtotal,status)",order:"created_at.desc",limit:"300"});if(channel)p.set("channel",`eq.${channel}`);return supabaseServerRequest(`/rest/v1/orders?${p}`);}
export async function listTransactions(type){const p=new URLSearchParams({select:"id,order_id,type,category,description,amount,payment_method,status,occurred_at,supplier:suppliers(id,name)",order:"occurred_at.desc",limit:"500"});if(type)p.set("type",`eq.${type}`);return supabaseServerRequest(`/rest/v1/financial_transactions?${p}`);}
export async function listAudit(){return supabaseServerRequest("/rest/v1/audit_logs?select=id,actor_id,actor_employee_id,actor_kind,action,entity_type,entity_id,metadata,created_at,employee:employees(full_name,username)&order=created_at.desc&limit=500");}
export async function createExpense(data,actor){const row=(await supabaseServerRequest("/rest/v1/financial_transactions?select=*",{method:"POST",prefer:"return=representation",body:{type:"saida",category:data.category,description:data.description,amount:data.amount,payment_method:data.paymentMethod,status:"confirmado",occurred_at:new Date().toISOString(),supplier_id:data.supplierId,actor_admin_id:actor.id}}))[0];await supabaseServerRequest("/rest/v1/audit_logs",{method:"POST",body:{actor_id:actor.id,actor_kind:"admin",action:"finance.expense_created",entity_type:"financial_transaction",entity_id:row.id,metadata:{amount:data.amount,description:data.description}}});return row;}
