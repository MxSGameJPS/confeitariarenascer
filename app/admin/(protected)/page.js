import AdminCatalog from "../components/AdminCatalog";

export const metadata = {
  title: "Painel Administrativo | Renascer",
  robots: { index: false, follow: false },
};

export default function AdminPage() {
  return <AdminCatalog />;
}
